const { Builder, By, until } = require('selenium-webdriver');
const chrome = require('selenium-webdriver/chrome');

(async function runTest() {
  console.log("[SKIPPED] Selenium test skipped manually.");
  process.exit(0);
})();
